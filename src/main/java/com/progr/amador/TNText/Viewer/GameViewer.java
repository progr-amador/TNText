package com.progr.amador.TNText.Viewer;

import com.googlecode.lanterna.terminal.Terminal;
import com.progr.amador.TNText.Model.Arena;
import com.progr.amador.TNText.Model.Elements.Element;
import com.progr.amador.TNText.TerminalGUI;

import java.util.List;

/* public class GameViewer extends Viewer<Arena> {

    public GameViewer(Arena arena) { super(arena);}




    private <T extends Element> void drawElements(TerminalGUI gui, List<T> elements, ElementViewer<T> viewer) {
        for (T element : elements)
            drawElement(gui, element, viewer);
    }

    private <T extends Element> void drawElement(TerminalGUI gui, T element, ElementViewer<T> viewer) {
        viewer.draw(element, gui);
    }


}
*/